#ifndef REG_GUESS_FC_WORKER_H
#define REG_GUESS_FC_WORKER_H

char * reg_guess_fc_worker(const char * surname,
			   const char * name,
			   const int year,
			   const int month,
			   const int day,
			   const int female,
			   const char * codice_catastale,
			   char * fiscal_code
    );

#endif /* REG_GUESS_FC_WORKER_H */
